describe "module", ->
  describe "create", ->
    xit "requires a module name as the first argument", ->
    xit "fails with a human readable error if the module directory already exists", ->
    xit "creates a module development harness by the given module name", ->
    xit "may be provided with --repo-url to create a managed module", ->
